spring:
  h2:
    console:
      enabled: true
      path: /console

  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=5

  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
    show-sql: false

